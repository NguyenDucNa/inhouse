{
  "name": "inhouse-order-frontend",
  "version": "1.0.0",
  "scripts": {
    "codegen": "dotenv -- turbo run codegen",
    "build": "dotenv -- turbo run build --filter=client",
    "dev": "dotenv -- turbo run dev",
    "test": "dotenv -- turbo run test",
    "lint": "turbo run lint"
  },
  "packageManager": "pnpm@8.13.1",
  "dependencies": {
    "@graphql-codegen/cli": "^5.0.0",
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.3.0",
    "turbo": "^1.11.2"
  },
  "devDependencies": {
    "pnpm": "^8.15.4",
    "typescript": "^5.3.3",
    "vite": "^5.1.1"
  }
}
